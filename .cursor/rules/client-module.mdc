---
description: 
globs: absrefined/client/*
alwaysApply: false
---
# Client Module: AudioBookShelf API Client

The [abs_client.py](mdc:absrefined/client/abs_client.py) module provides a client for interacting with AudioBookShelf servers. Key capabilities include:

- Authentication using API Key (configured via `config.toml`)
- Retrieving library and item metadata
- Getting chapter information for audiobooks
- Downloading audio files
- Updating chapter markers

The client handles API request formatting, authentication header management, response parsing, and error handling for all AudioBookShelf API interactions, using settings loaded from `config.toml`, see [config-handling.mdc](mdc:.cursor/rules/config-handling.mdc).
